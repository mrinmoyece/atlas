# Template only. Replace every placeholder before applying:
#   cp k8s/secrets.example.yaml k8s/secrets.local.yaml
#   kubectl apply -f k8s/secrets.local.yaml
# Keep secrets.local.yaml out of version control. Install the Prometheus
# Operator CRDs before applying servicemonitor.yaml; provision a default
# StorageClass (or set storageClassName on the PVC) before deployment.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: atlas-secrets
type: Opaque
stringData:
  anthropic-api-key: REPLACE_WITH_PROVIDER_KEY
  # key_id:sha256(raw_key):roles
  api-keys: REPLACE_WITH_KEY_ID_HASH_AND_ROLES
  # Raw key matching an entry in api-keys with run:read permission.
  prometheus-bearer-token: REPLACE_WITH_PROMETHEUS_BEARER_TOKEN
